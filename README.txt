Public Assets Folder
====================
This folder contains static files that are directly served by the web server.

Contains:
- Favicon files
- Static images
- Manifest files
- Any other public assets that don't need to be processed

These files are accessed directly via URLs without bundling or processing.
